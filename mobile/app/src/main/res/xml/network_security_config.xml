<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only: allow cleartext (plain HTTP) to the local Laravel server during
  development. The real phone reaches the laptop via `adb reverse` at
  127.0.0.1:8000; an emulator uses 10.0.2.2. Everything else stays HTTPS-only.
  Production builds point the base URL at an https:// host and this exemption
  does not apply.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
